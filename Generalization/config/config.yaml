# Configuration settings

# Hyparameters
learning_rate : 0.01
momentum      : 0.9
epochs        : 10
batch_size    : 64
val_split     : 0.1
step_size     : 6
gamma         : 0.1
train_dataset : 'SVHN' # or 'MNIST'
loss_function : 'CE' # or 'LSCE'
optimizer_    : 'SGD' # or 'Adam'
augmentation  : False
use_batch_norm: True
pre_train     : False
model_name    : 'model'
# Random seed
SEED          : 42

